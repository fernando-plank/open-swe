# ------------------Github App Secrets-----------------
# GitHub app client secrets. Used for the GitHub OAuth login flow.
NEXT_PUBLIC_GITHUB_APP_CLIENT_ID=""
GITHUB_APP_CLIENT_SECRET=""

# GitHub App redirect URI - Update based on deployment environment:
# Development: http://localhost:3000/api/auth/github/callback
# Vercel: https://your-vercel-app.vercel.app/api/auth/github/callback
# Render: https://your-render-app.onrender.com/api/auth/github/callback
# This value should match the redirect URL you have configured in your GitHub app settings.
GITHUB_APP_REDIRECT_URI="http://localhost:3000/api/auth/github/callback"

# GitHub App name - Update for production deployment:
# Development: "open-swe-dev"
# Production: "open-swe" (or your production app name)
GITHUB_APP_NAME="open-swe-dev"
GITHUB_APP_ID=""
# App secret key. Should be multi-line.
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----
...add your private key here...
-----END RSA PRIVATE KEY-----
"


# ------------------------API URLs------------------------
# The API URL of the proxy route in the Next.js app. This route forwards
# requests to the LangGraph server, injecting some secrets.
# Development: http://localhost:3000/api
# Vercel: https://your-vercel-app.vercel.app/api
# Render: https://your-render-app.onrender.com/api
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# The API URL of the LangGraph server. Used in the proxy route to forward
# requests to the LangGraph server.
# Development: http://localhost:2024
# Vercel: https://your-langgraph-service.com (external service URL)
# Render: https://your-langgraph-service.onrender.com
LANGGRAPH_API_URL="http://localhost:2024"

# ------------------------Security------------------------
# Encryption key for secrets (32-byte hex string for AES-256)
# Should be the same value as the one used in the agent app, so that secrets
# encrypted in the web app can be decrypted in the agent.
# Generate with: openssl rand -hex 32
SECRETS_ENCRYPTION_KEY=""

